import DocCardList from '@theme/DocCardList';

# Operations

Operating Jupyter MCP Server: what a tool call returns and how to address a
cell, what the server can be told to do and how to see it, and the seams —
hooks, extensions, audit — for changing it without forking it.

<DocCardList />
